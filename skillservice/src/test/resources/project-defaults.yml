swarm:
  datasources:
    data-sources:
      skillservice_db:
        driver-name: h2
        connection-url: jdbc:h2:~/test
        user-name: root
        password: secret
        flush-strategy: EntirePool
        background-validation: true
